# Gemini API Key — required for LLM features (classification, generation, judge)
# Get one at: https://aistudio.google.com/apikey
# If omitted, the system runs in MOCK mode with deterministic local fallbacks.
GEMINI_API_KEY=your_api_key_here
